{
  "name": "securesouls.com",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "git config --global url.\"https://${GH_TOKEN}@github.com/\".insteadOf \"https://github.com/\" && git config --global url.\"https://${GH_TOKEN}@github.com/\".insteadOf \"git@github.com:\" && git submodule update --init --recursive && echo '--- STRUKTURA FOLDERÓW PRZED BUILDEM ---' && find . -maxdepth 2 -not -path '*/.*' && npm run type-check && rm -rf dist && mkdir -p dist && esbuild index.ts */index.ts --bundle --minify --format=esm --outdir=dist --outbase=. --target=esnext --legal-comments=none --drop:console --drop:debugger && find . -maxdepth 2 -not -path '*/.*' -not -path './dist*' -not -path './node_modules*' -type d -exec cp -r {} dist/ \\; && find dist -name '*.ts' -delete",
    "deploy": "npx wrangler deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/8989denis/SecureSouls.com.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/8989denis/SecureSouls.com/issues"
  },
  "homepage": "https://github.com/8989denis/SecureSouls.com#readme",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260118.0",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3",
    "wrangler": "^4.59.2"
  }
}
